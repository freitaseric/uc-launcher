export function HomeScreen() {
	// useEffect(() => {
	// 	invoke('set_activity', { state: 'Página Inicial' }).catch(console.error)
	// }, [])

	return (
		<section className="flex items-center justify-center h-screen w-screen">
			<h1>Página Inicial</h1>
		</section>
	)
}
